# config.py

# This dictionary contains the database configuration used to establish a connection with the MySQL database.
# You can modify these settings (host, user, password, database) according to your environment.
DB_CONFIG = {
    'host': 'localhost', # The hostname where MySQL server is running.
    'user': 'vaxuser', # The hostname where MySQL server is running.
    'password': 'vaxpass123', # The password for the MySQL user.
    'database': 'vaccination_system' # The specific database to use.
}